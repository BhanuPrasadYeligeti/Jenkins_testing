pipeline {
    agent any

    stages {
        stage('Clone') {
            steps {
                echo "Code cloned successfully"
            }
        }

        stage('Build') {
            steps {
                echo "Running build stage"
            }
        }
    }
}
